<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/security https://www.springframework.org/schema/security/spring-security.xsd">


	<security:http auto-config="true" use-expressions="true">
	    
		<security:intercept-url pattern="/admin" access="hasRole('ROLE_ADMIN')"/>
		<security:intercept-url pattern="/vip" access="hasRole('ROLE_USER')"/>
		<security:intercept-url pattern="/**" access="permitAll"/>
		<security:intercept-url pattern="/register" access="permitAll"/>
	        
	    <!-- Configure form login -->
	    <security:form-login 
	    	login-page="/loginP"
	        login-processing-url="/login"
	        default-target-url="/main"
	        username-parameter="id"
	        password-parameter="pwd" />
	    
	    <!-- Configure logout -->
	    <security:logout logout-success-url="/main" />
	        
	</security:http>
    
	<security:authentication-manager>
		<security:authentication-provider>
		    <security:user-service>
			    <security:user name="admin" password="{noop}1111" authorities="ROLE_USER, ROLE_ADMIN" />
				<security:user name="guest" password="{noop}1111" authorities="ROLE_USER" />
				<security:user name="user" password="{noop}1111" authorities="ROLE_USER" />
			</security:user-service>
		</security:authentication-provider>
	</security:authentication-manager>
	
</beans>
